# Retired 2026-09-16: Apple traffic follows the existing Sukka resources.
# Empty compatibility endpoint for old URLs; no active rules or policy override.
# Remove this RULE-SET reference from the profile; do not reactivate it.
