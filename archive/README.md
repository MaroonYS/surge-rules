# Archive

只有经配置所有者明确确认不再加载的域名，才应移动到此目录。

归档文件不得被 `surge-main.conf` 引用；自动校验器会拒绝任何此类引用。
当前没有已确认停用的域名。

旧版 `Identity`、`Finance`、`GoogleVoice-*`、`Private` 与 `Apple-Push` 策略组示例
已经从可复制的 `snippets/` 中删除。当前主规则直接引用固定策略，继续保留这些示例
容易重新引入不存在的策略名；需要追溯时使用 Git 历史，不在归档目录保存可误复制的
配置段。
