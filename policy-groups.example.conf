# Merge these entries into the existing [Proxy Group] section.
# Keep the quoted policy name if your regional group contains spaces.

Private-Relay = select, "United States", no-alert=0, hidden=0
Apple-AI = select, "United States", AIGC, no-alert=0, hidden=0
