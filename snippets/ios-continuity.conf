# AirDrop/Handoff-priority mode. Merge these keys into the existing [General]
# section; do not add a second section. Do not combine this with
# APNs remains outside Surge and uses the physical path.
include-all-networks = false
include-local-networks = false
include-apns = false
include-cellular-services = false
